unit Click_Spectral_config;

uses Click_Spectral_types;

const
    _SPECTRAL_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _SPECTRAL_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _SPECTRAL_POLL_ENABLE : uint8_t = 0; // poll enabled
    _SPECTRAL_CALLBACK_ENABLE : uint8_t = 1; // calback disabled
    _SPECTRAL_UART_CFG : array[4] of uint32_t = (115200, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.